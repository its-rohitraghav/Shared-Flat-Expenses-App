# Database connection string for PostgreSQL (required for Vercel deployment)
# You can get a free PostgreSQL database from services like Neon.tech, Supabase, or Vercel Postgres.
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require
DATABASE_URL="postgresql://user:password@host.region.db.com/dbname?sslmode=require"
